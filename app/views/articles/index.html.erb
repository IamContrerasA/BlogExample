<h1>Articles</h1>

Our blog has <%= Article.public_count %> public articles and counting! <%= @extra_title%>

<ul class="list-group">
  <h4>Public Articles</h4>
  <% @public_articles.each do |article| %>
    <li class="list-group-item"> <%= link_to article.title, article %> </li>
  <% end %>

  <h4>Private Articles</h4>
  <% @private_articles.each do |article| %>
    <li class="list-group-item"> <%= link_to article.title, article %> </li>
  <% end %>

  <h4>Achieved Articles</h4>
  <% @achieved_articles.each do |article| %>
    <li class="list-group-item"> <%= link_to article.title, article %> </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path, class: "btn btn-primary" %>